<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Electrum sockets (inside libwallet_ffi.so), explorer calls, fonts.
         Flutter injects this only into debug/profile manifests, not release. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Fingerprint unlock of the vault. biometric_storage merges this too;
         declared here so the requirement survives a plugin swap. -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>
    <!-- API 24-27: androidx.biometric authenticates through FingerprintManager. -->
    <uses-permission android:name="android.permission.USE_FINGERPRINT" android:maxSdkVersion="27"/>
    <!-- QR scanning: PSBT/PSET and key exchange with air-gapped signers.
         mobile_scanner merges CAMERA too; declared here so the permission
         survives a plugin swap, like USE_BIOMETRIC above. Optional feature:
         a phone without a camera pastes or loads a file instead. -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    <!-- allowBackup=false: the vault, registry and sled databases must never
         be copied by Auto Backup or device-to-device transfer. -->
    <application
        android:label="Templar Wallet"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- templar:// deep links from Templar Protocol (mirrors CFBundleURLSchemes
                 on macOS); delivered through app_links. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="templar"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- canLaunchUrl(https://…) on API 30+ (explorer / mempool links). -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
