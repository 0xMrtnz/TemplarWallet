<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark-mode halves of the Base* styles in values/styles.xml; LaunchTheme
         and NormalTheme themselves are defined once there (and per API level
         in values-v27 / values-v31) and inherit these. -->

    <!-- Theme applied to the Android Window while the process is starting when
         the OS's Dark Mode setting is on -->
    <style name="BaseLaunchTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- AppCompat parents paint the bars with colorPrimaryDark (grey); the
             Flutter shell owns the bars and draws edge-to-edge under them. -->
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="BaseNormalTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- AppCompat parents paint the bars with colorPrimaryDark (grey); the
             Flutter shell owns the bars and draws edge-to-edge under them. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/window_background</item>
    </style>
</resources>
