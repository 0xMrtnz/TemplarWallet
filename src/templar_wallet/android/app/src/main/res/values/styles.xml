<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Two-level layout: Base* styles carry everything that differs by
         light/dark (parent + colours, overridden in values-night), the public
         LaunchTheme/NormalTheme only add per-API items in values-v27 and
         values-v31 — so no values-night-vNN copies are needed. -->

    <!-- Theme applied to the Android Window while the process is starting when
         the OS's Dark Mode setting is off. Only the starting window ever uses
         it: FlutterActivity switches to NormalTheme in onCreate. -->
    <style name="BaseLaunchTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- AppCompat parents paint the bars with colorPrimaryDark (grey); the
             Flutter shell owns the bars and draws edge-to-edge under them. -->
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:navigationBarColor">@color/splash_background</item>
        <!-- Show a splash screen on the activity. Automatically removed when
             the Flutter engine draws its first frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <!-- Theme applied to the Android Window as soon as the process has started.
         This theme determines the color of the Android Window while your
         Flutter UI initializes, as well as behind your Flutter UI while its
         running.

         This Theme is only used starting with V2 of Flutter's Android embedding. -->
    <style name="BaseNormalTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- AppCompat parents paint the bars with colorPrimaryDark (grey); the
             Flutter shell owns the bars and draws edge-to-edge under them. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <style name="LaunchTheme" parent="BaseLaunchTheme"/>
    <style name="NormalTheme" parent="BaseNormalTheme"/>
</resources>
