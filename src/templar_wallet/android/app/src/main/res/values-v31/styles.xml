<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- API 31+: the system splash screen replaces the windowBackground
         starting window. Black ground, the crimson cross on a badge-dark
         circle. splash_icon is rendered at 288dp with the cross at 50% because
         the system masks a non-adaptive icon to the inner 2/3 circle (same
         geometry as the adaptive launcher icon). NormalTheme resolves from
         values-v27. -->
    <style name="LaunchTheme" parent="BaseLaunchTheme">
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/splash_icon_background</item>
    </style>
</resources>
