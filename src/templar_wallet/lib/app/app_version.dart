/// The version About shows. It must equal `version:` in pubspec.yaml (the
/// part before `+`) — test/app_version_test.dart fails the build otherwise,
/// so a release bump cannot leave About behind again.
const String kAppVersion = '0.1.0';
